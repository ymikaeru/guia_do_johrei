.claude
*.bak.*
*.bak
.agent

# Credenciais (NUNCA commitar)
.env
.env.local
.env.*.local

# Node (caso scripts/ use npm install no futuro)
node_modules/

# Reports gerados por scripts (auto-gerados, não commitar)
unmatched_report.json